<%
t_allow = []

@l_trusted_nets.each do |lnet|
  t_addr,t_mask = lnet.split('/')

  t_str = "restrict #{t_addr}"

  if t_mask
    t_str += " mask #{t_mask}"
  end

  unless @rules
    t_str += " #{Array(@rules).join(' ')}"
  end

  t_allow.push(t_str.strip)
end
-%>
<%= t_allow.join("\n") %>
